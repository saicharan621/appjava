pipeline {
    agent any

    stages{

        stage('git checkou'{
            steps{
                git branch: 'main', url: 'https://github.com/saicharan621/appjava.git'
            }
        })
    }
}